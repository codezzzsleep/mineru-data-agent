# Public Real Documents

This directory stores public real-document inputs used to test the submission beyond synthetic fixtures.

The files are downloaded from official public sources listed in `manifest.json`. Generated submission artifacts live under `submission_artifacts/public_real_cases/`.

Boundary: these are public documents with lightweight human labels. They are not private customer data and do not claim full OCR character-level benchmarking.
